import React from "react";

const Loading = () => {
  return (
    <div className="spinner-container h-full flex items-center justify-center">
      <div className="spinner"></div>
    </div>
  );
};

export default Loading;
